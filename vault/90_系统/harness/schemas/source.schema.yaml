type: source
paths:
  - 11_原材料
required:
  type:
    kind: enum
    values: [source]
  format:
    kind: enum
    values: [book, paper, article, video, audio, podcast, report, news, x_post, course]
  title:
    kind: string
  created:
    kind: date
  status:
    kind: enum
    values: [captured, extracted, summarized, reviewed, linked, archived, ignored]
optional:
  author:
    kind: string
  source_url:
    kind: string
  source_location:
    kind: string
  related_topics:
    kind: list
  related_projects:
    kind: list
  importance:
    kind: enum
    values: [low, medium, high]
  summary_file:
    kind: string
  extract_file:
    kind: string
  tags:
    kind: list
